_format_version: "3.0"
_transform: true

services:
  - name: search-service
    url: http://search-service:3000
    routes:
      - name: search-v1
        paths:
          - /api/v1/search
          - /api/v1/trains
        strip_path: false
    plugins:
      - name: rate-limiting
        config:
          minute: 100
          policy: local
      - name: cors
      - name: jwt

  - name: booking-service
    url: http://booking-service:3000
    routes:
      - name: booking-v1
        paths:
          - /api/v1/booking
          - /api/v1/ticket
        strip_path: false
    plugins:
      - name: rate-limiting
        config:
          minute: 30
          policy: local
      - name: acl
      - name: jwt
