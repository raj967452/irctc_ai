'use client';
import { useMemo, useState } from 'react';
import SeatSelector from '@/components/SeatSelector';
const savedPassengers = ['Asha Kumar', 'Rahul Singh', 'Maya Iyer'];
export function BookingExperience() { const [seconds, setSeconds] = useState(480); const fare = useMemo(() => 1760 + savedPassengers.length * 40, []); return <section className="grid gap-5 md:grid-cols-2"><div className="rounded-3xl bg-white p-6 shadow"><h2 className="text-2xl font-black">Smart passenger form</h2><p>Auto-fill from saved passengers: {savedPassengers.join(', ')}</p><p>OCR for ID card scanning uploads ID images to a secure extraction service.</p><p>CAPTCHA-less verification uses behavioral analysis and device risk scoring.</p><p>Session expiry countdown: {Math.floor(seconds / 60)}:{String(seconds % 60).padStart(2, '0')}</p><button className="mt-3 rounded-full bg-blue-700 px-4 py-2 text-white" onClick={() => setSeconds(value => Math.max(0, value - 30))}>Simulate timer</button></div><div className="grid gap-4"><SeatSelector /><div className="rounded-3xl bg-white p-6 shadow"><h2 className="text-xl font-black">Real-time fare calculation</h2><p>Fare: ₹{fare}</p><label><input type="checkbox" /> Food catering selection</label><br/><label><input type="checkbox" /> Insurance options</label></div></div></section>; }
