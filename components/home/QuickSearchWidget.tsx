'use client';
import { useBookingStore } from '@/store/useBookingStore';
const suggestions = ['Mumbai for business travel', 'Varanasi pilgrimage weekend', 'Goa coastal getaway', 'Jaipur heritage circuit'];
export function QuickSearchWidget() { const { searchParams, setSearchParams } = useBookingStore(); const update = (key: keyof typeof searchParams, value: string) => setSearchParams({ ...searchParams, [key]: value }); return <section className="rounded-3xl bg-white p-6 shadow-xl"><h2 className="text-2xl font-black">Quick search</h2><div className="mt-4 grid gap-3 md:grid-cols-4"><input aria-label="From" placeholder="From" className="rounded-xl border p-3" onChange={e => update('from', e.target.value)} /><input aria-label="To" placeholder="To" className="rounded-xl border p-3" onChange={e => update('to', e.target.value)} /><input aria-label="Date" type="date" className="rounded-xl border p-3" onChange={e => update('date', e.target.value)} /><select aria-label="Class" className="rounded-xl border p-3" onChange={e => update('travelClass', e.target.value)}><option>3A</option><option>2A</option><option>SL</option><option>CC</option></select></div><h3 className="mt-5 font-black">AI-powered destination suggestions</h3><div className="mt-2 flex flex-wrap gap-2">{suggestions.map(item => <button className="rounded-full bg-blue-50 px-3 py-2 text-sm font-bold text-blue-800" key={item}>{item}</button>)}</div></section>; }
